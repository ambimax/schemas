{
    "$schema": "http://json-schema.org/draft-07/schema",
    "description": "Record type for categories.",
    "type": "object",
    "properties": {
        "entity": {
            "type": "string",
            "enum": ["category"]
        },
        "id": {
            "type": "string"
        },
        "operation": {
            "type": "string",
            "enum": ["save", "delete"]
        },
        "data": { "$ref": "../entities/category.json" }
    },
    "required": [
        "entity", "id", "operation", "data"
    ],
    "additionalProperties": false
}
