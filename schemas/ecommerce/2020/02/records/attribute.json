{
    "$schema": "http://json-schema.org/draft-07/schema",
    "description": "Record type for attributes.",
    "type": "object",
    "properties": {
        "entity": {
            "type": "string",
            "enum": ["attribute"]
        },
        "id": {
            "type": "string",
            "pattern": "^.+$"
        },
        "operation": {
            "type": "string",
            "enum": ["save", "delete"]
        },
        "data": { "$ref": "../entities/attribute.json" }
    },
    "required": [
        "entity", "id", "operation", "data"
    ],
    "additionalProperties": false
}
